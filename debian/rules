#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

export SETUPTOOLS_SCM_PRETEND_VERSION=2.0.0
export PYBUILD_NAME=rst.linker

%:
	dh $@ --with python2,python3 --buildsystem=pybuild --test-pytest
